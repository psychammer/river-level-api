Install dependencies:

bashpip install -r requirements.txt

Run the API locally:

uvicorn river_level_api:app --reload --host 0.0.0.0 --port 8000

Test the API:

python test_api.py
Step 3: Docker Deployment
Build and run with Docker:
bash# Build the image
docker build -t river-level-api .

# Run with docker-compose
docker-compose up -d